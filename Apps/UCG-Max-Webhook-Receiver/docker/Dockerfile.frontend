FROM node:18-alpine

WORKDIR /app

COPY frontend/package*.json ./
RUN npm install

COPY frontend/ .
RUN npm run build

# Since backend serves frontend, this might not be needed, but for separate build
FROM nginx:alpine
COPY --from=0 /app/dist /usr/share/nginx/html